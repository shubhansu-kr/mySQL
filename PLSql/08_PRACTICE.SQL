-- cREATE A TRIGGER TO INSERT DATA IN UPPERCASE ONLY 

CREATE OR REPLACE TRIGGER UPPERCASETRIGGER
    BEFORE 
        INSERT
        ON T21
BEGIN
    :NEW.SNAME := UPPER(:NEW.SNAME);
END;

-- CREATE TABLE T21 (
--     SNAME VARCHAR(21)
-- );


-- dELETE ALL THE DATA OF STUDENTS IN STUDENT TABLE IF 
-- BRANCH IS DELETED

/* 
BRANCH: TABLE 

STUDENT: TABLE
*/

CREATE TABLE BRANCH(
    BNAME VARCHAR(26) PRIMARY KEY, 
    BID VARCHAR(10)
);

CREATE TABLE STD21 (
    SNAME VARCHAR(21), 
    SROLL NUMBER(10),
    BNAME VARCHAR(26) REFERENCES BRANCH(BNAME)
);

CREATE OR REPLACE TRIGGER DELETESTUDENT
    BEFORE 
    DELETE
    ON BRANCH 
BEGIN 
    DELETE FROM STD21 WHERE BNAME = OLD.BNAME
END;